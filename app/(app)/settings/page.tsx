import React from "react";
import SettingsContainer from "@/components/modules/settings/components/SettingsContainer/SettingsContainer";

const SettingsPage: React.FC = () => (
  <SettingsContainer/>
);

export default SettingsPage;
