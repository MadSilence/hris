import { ForgotPasswordContainer } from "@/components/modules/passwordReset";

/** Where "Forgot Password?" on the sign-in form leads. */
export default function ForgotPasswordPage() {
  return <ForgotPasswordContainer />;
}
